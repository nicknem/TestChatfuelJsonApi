<div class="container-fluid">
  <h1>FB users list:</h1>
  <% if @fb_users.empty? %>
    <p>No fbusers yet, create one: <%= link_to "Créer un fb_user",
    new_fb_user_path %></p>
  <% else %>
    <ul>
      <% @fb_users.each do |fb_user| %>
        <li><%= fb_user.fb_first_name  %> <%= fb_user.fb_last_name %> <%= fb_user.fb_gender %> <%= fb_user.fb_locale %></li>
      <% end %>
    </ul>
  <% end %>
</div>
